import { User } from "@blocknote/core/comments";
export declare function useUser(userId: string): User | undefined;
/**
 * Bridges the UserStore to React using useSyncExternalStore.
 */
export declare function useUsers(userIds: string[]): Map<string, User>;
